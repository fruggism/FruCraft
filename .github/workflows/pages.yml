# Tests every branch, and publishes the app to GitHub Pages from whichever
# branch is the repository's default one. The first run also switches Pages on
# by itself, so there is no setting to remember.
name: Test e pubblica Cube-Atlas

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # The project has no dependencies, so there is nothing to install.
      - run: npm test

  deploy:
    name: Pubblica su Pages
    needs: test
    # The github-pages environment only accepts deployments from the default
    # branch, so don't even try from the others.
    if: github.event_name != 'pull_request' && github.ref_name == github.event.repository.default_branch
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        with:
          # Switches Pages on the first time this runs, so the site does not
          # need anyone to flip a setting by hand.
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          # Static files: no build step, nothing to compile.
          path: web
      - id: deployment
        uses: actions/deploy-pages@v4
